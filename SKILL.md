---
name: douyin-publish
description: 抖音图文自动发布技能。当用户要求把 A 股涨停复盘日报发布到抖音、说"发抖音/抖音发布/发图文"、或询问抖音发布进度与状态时使用。覆盖：全图卡片生成、CDP 自动上传、标题简介填充、预览确认、发布提交全流程。发布形态为全部内容图片化（7~10 张竖版卡），不是淘股吧那种半文半图。
agent_created: true
---

# 抖音图文自动发布（douyin-publish）

把 `C:\Users\lhj\Desktop\复盘日记\复盘工作台\reports\` 的涨停复盘日报转成**全内容图片卡组**发布到抖音（creator.douyin.com 图文作品）。半自动模式：脚本做上传与填表，用户只做"启动浏览器（首次）+ 发布前过目 + 点头"三个动作。

## 铁律（先读）

1. **`--post` 发布必须获得用户当轮明确确认**（如回复"发"）。填表 ≠ 发布。抖音作品发布后可删除但已有曝光，仍按"发前必过目"执行。
2. **发布形态是全图卡组**（用户 2026-09-23 拍板）：所有内容图片化，标题/简介只放短文字+话题 `#涨停复盘 #短线打板`。**不要**退回"一半文字一半图"的淘股吧混排形式。
3. **专用浏览器必须由用户双击 `start_douyin_browser.bat` 启动**（CDP 9224、独立 profile `~/.workbuddy/chrome-douyin-profile`）——执行环境会回收 AI 启动的所有进程，只有用户亲手启动的进程能常驻。
4. 用户账号密码永远不要索要或经手；首次扫码登录后登录态持久。
5. **抖音风控强于淘股吧**（滑块/IP 敏感）：遇滑块或验证码脚本会截图退出，让用户在窗口里人工过掉后重跑；不要重试硬闯。
6. 与 `taoguba-publish` 互不干扰：两套浏览器、两个端口（9223 / 9224）、两个 profile。

## 内容合规(2026-09-23 限流反馈后新增,先读)

0922/0923 连发两条同模板图文后,收到抖音「批量发布同质化低质内容」限流通知。已定案改造,生成层全部内置:

1. **标题/简介/卡片一律不出现个股名与"明日关注:XX"荐股指向**——卡3 的个股区已改为"操作纪律"话术(`STRATEGY` 表,按情绪给纪律而非荐股);**卡片上的股票名全部脱敏为两字缩写**(`mask_name`/`mask_names`/`mask_free_text`:华瓷股份→华瓷,大亚圣象→大亚,自由文本里"名字(代码)"模式同步缩写,ST 前缀保留)
2. **标题走 `TITLE_POOL`**(按情绪选风格、按日期确定性轮换),不再用"MMDD复盘|涨停XX家"固定模板;生成后仍受 20 字符上限约束
3. **话题按星期轮换**(`TOPICS_BY_WD`),不再固定"#涨停复盘 #短线打板"
4. 简介自然语言化(盘面手记体),尾带"个人复盘记录,不构成投资建议"
5. **发布节奏:隔 2~3 天一条;遇限流通知停发 3~5 天**——连续日更是本次限流主因
6. 页眉徽章按日期轮换(`HEADER_BADGES`),卡片模板观感每日有差异

## 前置检查（按顺序）

1. **日报 + JSON 存在**：`复盘工作台\data\YYYYMMDD.json`（说"今天"用今天，周日用最近一个交易日）。不存在 → 提示先跑 `a-stock-limitup-review`。
2. **图片已生成**：`复盘工作台\reports\douyin\YYYYMMDD\0*.png`（7 张）。没有 → 先跑第 1 步生成。
3. **浏览器在线**：GET `http://127.0.0.1:9224/json` 返回 200。不在线 → 让用户双击 `复盘工作台\start_douyin_browser.bat`，等用户确认窗口已开。
4. **登录态**：脚本检测到未登录会截 `YYYYMMDD-待登录.png` 并退出 → 请用户在窗口里扫码登录，登录后重跑。

## 标准流程

所有 python 命令用 venv 解释器：`C:\Users\lhj\.workbuddy\binaries\python\envs\default\Scripts\python.exe`。命令输出一律重定向到文件后 Read 读取（本机 shell stdout 不可靠）。

1. **生成图片卡组**：`python 复盘工作台\make_douyin_images.py YYYYMMDD`
   - 产出 `reports\douyin\YYYYMMDD\01_cover.png ~ 07_risk.png`（1080×1440 竖版，7 张，张数固定但内容随 JSON 浮动）+ 同名 .html（调试用）
   - 字段驱动，纯本地 Chrome headless 截图，无 CDN 无风控问题
   - 若用户要求调整视觉：改 `make_douyin_images.py` 内对应 card 函数重跑即可
2. **上传+填表+预览**：`python 复盘工作台\douyin_publish.py YYYYMMDD`
   - 自动：进图文上传模式 → CDP `DOM.setFileInputFiles` 批量上传 7 张 → 填标题（≤19 字自动生成）与简介（情绪/指标/预判/关注票+话题）→ 截图 `YYYYMMDD-发布预览.png`
   - 把截图展示给用户 + 提示用户到 Chrome 窗口核对图片顺序与文案
   - 标题/简介文案在跑之前先打印出来给用户看
   - 变体参数：`--refill`（页面已有图，只重填标题/简介）、`--force`（配合页面"清空并重新上传"按钮手工清空后重传）
3. **配乐（人工）**：请用户在窗口点『选择音乐/修改音乐』→ 在面板里搜歌并点选 → 点『使用』。用户完成后跑核验脚本确认「选择音乐」行已变为所选曲目。
4. **发布**：用户当轮明确说"发"后：`python 复盘工作台\douyin_publish.py YYYYMMDD --post`
   - 点"发布"按钮 → 8 秒后截图 `YYYYMMDD-发布结果.png`
   - **首次会弹短信二次验证**：让用户在窗口点"获取验证码"→填短信码→点"验证",完成后核验 URL 是否跳 `content/manage` 且作品列表出现「7张|标题」
   - 成功判据满足即交付:提示作品链接在抖音 App「我→作品」里复制
5. **收尾**：发布后 tab 停在作品管理页。

## 实测已知事实（2026-09-23 首跑 0922 验证）

- **标题上限 20 字符**（页面提示"标题内容仅支持20个字符"，不是 30）。gen_title 生成 ≤19 字，格式 `922复盘|分歧·涨停65家,明日退潮`（月份日期去前导零省字）。
- **简介多行会被编辑器规整为单段**：insertParagraph / insertHTML+`<br>` 都被拦，程序注入的换行不保留。内容完整即可，用户在意分行需在窗口手动回车。
- **上传成功的判据**：页面出现"已添加7张图片" + `document.images.length` 大增（6→48）。脚本有防重复上传（检测"已添加N张图片"即跳过）。
- **发文助手**：抖音自带发布前检测（右下角），显示"作品未见异常"即通过。
- **坑：焦点残留**——上次 insertText 后焦点留在简介框，重跑时 `e!==document.activeElement` 会把唯一候选跳过导致 NO_MATCH（已修：候选选择去掉该条件，无条件兜底取第一个 contenteditable）。

## 排障（先读诊断再修）

- 所有页面诊断落在 `reports\douyin\YYYYMMDD\_publish_diag.json`：`form_after_fill.fields`（输入框 placeholder/class）、`buttons`（按钮文案与 disabled）、`upload.file_inputs`（file input 的 accept/multiple）。
- 常见失败：
  - `NO_FILE_INPUT` → 未切到图文 tab 或抖音改版，看 `switch.tab` 与 `form_after_fail`
  - 填文本 `NO_MATCH` → 抖音改了输入框结构，按 `form_after_fill.fields` 的 placeholder 改 `set_text` 里候选 selector
  - `DISABLED` / 点了没反应 → 校验没过（图片没传完/标题为空），看预览截图与 `before_post`
- 上传判断以预览截图为准（`document.images.length` 增量只是参考）。
- 抖音 UI 升级频繁，锚点失效属预期，按诊断修 selector，不要重写流程。

## 当前能力边界（2026-09-23 · 全链路已打通）

- ✅ **0922 图文首条实战发布成功**（13:42,审核中）:7 图全图卡 + 标题 19/20 + 简介 + 配乐《清明上河图(同花顺进行曲)》周深深版 + 话题 #涨停复盘 #短线打板。
- ✅ 发布成功判据:`--post` 后 URL 跳 `content/manage?enter_from=publish` + 作品列表出现「7张 | 标题…」+ 发布时间。**管理页不暴露作品链接**,分享链接在抖音 App「我→作品」里拿。
- ⚠️ **`--post` 每次都可能弹短信二次验证**(0922、0923 连续两天都弹,非仅首次):人工输入手机验证码后流程自动走完,发布判据照常核验。
- ⚠️ **--refill/重填简介必须强力清空**:execCommand selectAll+delete 可能删不净导致新旧拼接(双份简介);脚本已实现"清空循环(execCommand → Ctrl+A/Delete 真实按键兜底)→ 检查残留 → 再插入"。
- ⚠️ **数据字段可能为 null**(如 premium_pct):gen_desc/gen_title 已做 None 安全显示("-"),不会再出现"None%"。
- ⚠️ **音乐环节定案为人工分工**(自动化不可靠):面板打开按钮(选择音乐/修改音乐)对 JS click 与 CDP 真实鼠标全部免疫;搜索框 native setter 填词时灵时不灵;结果集每次刷新都变(虚拟列表+排序漂移)。**流程:用户在面板里搜歌并点选 → 点『使用』→ 跑核验**(配乐行/标题/简介/7图/发文助手全绿后请用户发"发")。
- ⚠️ **曲名括号是全角（）**:页面曲名如「清明上河图（同花顺进行曲）」,匹配串必须用全角括号,半角永远 NOT_FOUND。
- ⚠️ 探索事故教训:搜索框兜底误抓主页标题框(已改为只认 placeholder="搜索音乐");pick 失败不得点"使用"(会把默认高亮歌误确认);面板关闭时 React keep-alive 保留隐藏 DOM,面板真伪判据 = `elementFromPoint` 命中;反复清空重填会打断搜索(空态时逐字符拟人重输可救)。
- ⏸ 未接入每日 18:00 定时任务(同淘股吧,先手动跑)。
- 参考:淘股吧同款 CDP 基建见 `taoguba-publish` 技能与 `tgb_publish.py`。
